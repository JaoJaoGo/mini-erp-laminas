<?php

declare(strict_types=1);

/** @var \Application\Entity\User|null $user */
$user = $this->user ?? null;

/** @var \Application\Entity\Product|null $product */
$product = $this->product ?? null;

/** @var \Application\Form\ProductForm $form */
$form = $this->form;

/** @var array<int, \Application\Entity\Category> $categories */
$categories = $this->categories ?? [];

$isEdit = $product !== null;
$title = $isEdit ? 'Editar produto' : 'Novo produto';

$form->prepare();

$nameElement = $form->get('name');
$descriptionElement = $form->get('description');
$imageElement = $form->get('image');
$priceElement = $form->get('price');
$stockElement = $form->get('stock');
$isActiveElement = $form->get('isActive');
$csrfElement = $form->get('csrf');

$selectedCategories = $form->get('categories')->getValue() ?? [];

if (!is_array($selectedCategories)) {
    $selectedCategories = [];
}

$categoriesMap = [];
foreach ($categories as $category) {
    $categoriesMap[(string) $category->getId()] = $category->getName();
}

$nameElement->setAttribute('style', 'width: 100%; padding: 12px 14px; border: 1px solid #d1d5db; border-radius: 8px; background: #fff; color: #111827;');
$descriptionElement->setAttribute('style', 'width: 100%; padding: 12px 14px; border: 1px solid #d1d5db; border-radius: 8px; background: #fff; color: #111827; resize: vertical;');
$imageElement->setAttribute('style', 'width: 100%; padding: 10px 12px; border: 1px solid #d1d5db; border-radius: 8px; background: #fff; color: #111827;');
$priceElement->setAttribute('style', 'width: 100%; padding: 12px 14px; border: 1px solid #d1d5db; border-radius: 8px; background: #fff; color: #111827;');
$stockElement->setAttribute('style', 'width: 100%; padding: 12px 14px; border: 1px solid #d1d5db; border-radius: 8px; background: #fff; color: #111827;');
$isActiveElement->setAttribute('style', 'width: 100%; padding: 12px 14px; border: 1px solid #d1d5db; border-radius: 8px; background: #fff; color: #111827;');

function renderFieldErrors(array $messages, $escapeHtml): string {
    if (empty($messages)) {
        return '';
    }

    $html = '<div style="margin-top: 6px; color: #b91c1c; font-size: 14px;">';

    foreach ($messages as $message) {
        if (is_array($message)) {
            foreach ($message as $nestedMessage) {
                $html .= '<div>' . $escapeHtml($nestedMessage) . '</div>';
            }
        } else {
            $html .= '<div>' . $escapeHtml($message) . '</div>';
        }
    }

    $html .= '</div>';

    return $html;
}
?>

<?= $this->partial('layout/header', ['user' => $user]) ?>

<main style="max-width: 1200px; margin: 40px auto; padding: 0 24px;">
    <div style="display: flex; align-items: center; justify-content: space-between; gap: 16px; margin-bottom: 24px; flex-wrap: wrap;">
        <h1 style="margin: 0; font-size: 32px; color: #111827;">
            <?= $this->escapeHtml($title) ?>
        </h1>

        <a
            href="<?= $this->url('product') ?>"
            style="
                display: inline-flex;
                align-items: center;
                justify-content: center;
                padding: 12px 18px;
                border-radius: 8px;
                background: #374151;
                color: #fff;
                text-decoration: none;
                font-weight: 600;
            "
        >
            Voltar
        </a>
    </div>

    <div style="background: #fff; border: 1px solid #e5e7eb; border-radius: 16px; padding: 32px; box-shadow: 0 10px 30px rgba(0, 0, 0, 0.06);">
        <?= $this->form()->openTag($form) ?>
            <?= $this->formHidden($csrfElement) ?>

            <div style="display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 20px;">
                <div style="grid-column: span 2;">
                    <label
                        for="<?= $this->escapeHtmlAttr($nameElement->getAttribute('id')) ?>"
                        style="display: block; margin-bottom: 8px; font-size: 14px; font-weight: 700; color: #374151;"
                    >
                        <?= $this->escapeHtml($nameElement->getLabel()) ?>
                    </label>

                    <?= $this->formText($nameElement) ?>
                    <?= renderFieldErrors($nameElement->getMessages(), [$this, 'escapeHtml']) ?>
                </div>

                <div>
                    <label
                        for="category-selector"
                        style="display: block; margin-bottom: 8px; font-size: 14px; font-weight: 700; color: #374151;"
                    >
                        Categorias
                    </label>

                    <select
                        id="category-selector"
                        style="
                            width: 100%;
                            padding: 12px 14px;
                            border: 1px solid #d1d5db;
                            border-radius: 8px;
                            background: #fff;
                            color: #111827;
                        "
                    >
                        <option value="">Selecione uma categoria</option>

                        <?php foreach ($categories as $category): ?>
                            <option value="<?= $this->escapeHtmlAttr((string) $category->getId()) ?>">
                                <?= $this->escapeHtml($category->getName()) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>

                    <div
                        id="selected-categories"
                        style="
                            margin-top: 12px;
                            display: flex;
                            flex-wrap: wrap;
                            gap: 10px;
                        "
                    >
                        <?php foreach ($selectedCategories as $categoryId): ?>
                            <?php
                            $categoryId = (string) $categoryId;
                            $categoryName = $categoriesMap[$categoryId] ?? null;
                            if ($categoryName === null) {
                                continue;
                            }
                            ?>
                            <div
                                class="selected-category-item"
                                data-category-id="<?= $this->escapeHtmlAttr($categoryId) ?>"
                                style="
                                    display: inline-flex;
                                    align-items: center;
                                    gap: 10px;
                                    padding: 10px 12px;
                                    border-radius: 999px;
                                    background: #eff6ff;
                                    border: 1px solid #bfdbfe;
                                    color: #1e3a8a;
                                    font-size: 14px;
                                    font-weight: 600;
                                "
                            >
                                <span><?= $this->escapeHtml($categoryName) ?></span>

                                <button
                                    type="button"
                                    class="remove-category-button"
                                    style="
                                        width: 24px;
                                        height: 24px;
                                        border: 0;
                                        border-radius: 999px;
                                        background: #dbeafe;
                                        color: #1d4ed8;
                                        font-weight: 700;
                                        cursor: pointer;
                                        line-height: 1;
                                    "
                                >
                                    ×
                                </button>

                                <input type="hidden" name="categories[]" value="<?= $this->escapeHtmlAttr($categoryId) ?>">
                            </div>
                        <?php endforeach; ?>
                    </div>
                </div>

                <div>
                    <label
                        for="<?= $this->escapeHtmlAttr($isActiveElement->getAttribute('id')) ?>"
                        style="display: block; margin-bottom: 8px; font-size: 14px; font-weight: 700; color: #374151;"
                    >
                        <?= $this->escapeHtml($isActiveElement->getLabel()) ?>
                    </label>

                    <?= $this->formSelect($isActiveElement) ?>
                    <?= renderFieldErrors($isActiveElement->getMessages(), [$this, 'escapeHtml']) ?>
                </div>

                <div style="grid-column: span 2;">
                    <label
                        for="<?= $this->escapeHtmlAttr($imageElement->getAttribute('id')) ?>"
                        style="display: block; margin-bottom: 8px; font-size: 14px; font-weight: 700; color: #374151;"
                    >
                        <?= $this->escapeHtml($imageElement->getLabel()) ?>
                    </label>

                    <?php if ($isEdit && $product?->getImagePath()): ?>
                        <div style="margin-bottom: 12px;">
                            <img
                                src="<?= $this->escapeHtmlAttr($product->getImagePath()) ?>"
                                alt="<?= $this->escapeHtmlAttr($product->getName()) ?>"
                                style="width: 160px; height: 160px; object-fit: cover; border-radius: 12px; border: 1px solid #e5e7eb;"
                            >
                        </div>
                    <?php endif; ?>

                    <?= $this->formFile($imageElement) ?>
                    <div style="margin-top: 8px; font-size: 13px; color: #6b7280;">
                        Envie JPG, JPEG, PNG ou WEBP com até 5MB.
                    </div>
                    <?= renderFieldErrors($imageElement->getMessages(), [$this, 'escapeHtml']) ?>
                </div>

                <div>
                    <label
                        for="<?= $this->escapeHtmlAttr($priceElement->getAttribute('id')) ?>"
                        style="display: block; margin-bottom: 8px; font-size: 14px; font-weight: 700; color: #374151;"
                    >
                        <?= $this->escapeHtml($priceElement->getLabel()) ?>
                    </label>

                    <?= $this->formText($priceElement) ?>
                    <?= renderFieldErrors($priceElement->getMessages(), [$this, 'escapeHtml']) ?>
                </div>

                <div>
                    <label
                        for="<?= $this->escapeHtmlAttr($stockElement->getAttribute('id')) ?>"
                        style="display: block; margin-bottom: 8px; font-size: 14px; font-weight: 700; color: #374151;"
                    >
                        <?= $this->escapeHtml($stockElement->getLabel()) ?>
                    </label>

                    <?= $this->formNumber($stockElement) ?>
                    <?= renderFieldErrors($stockElement->getMessages(), [$this, 'escapeHtml']) ?>
                </div>

                <div style="grid-column: span 2;">
                    <label
                        for="<?= $this->escapeHtmlAttr($descriptionElement->getAttribute('id')) ?>"
                        style="display: block; margin-bottom: 8px; font-size: 14px; font-weight: 700; color: #374151;"
                    >
                        <?= $this->escapeHtml($descriptionElement->getLabel()) ?>
                    </label>

                    <?= $this->formTextarea($descriptionElement) ?>
                    <?= renderFieldErrors($descriptionElement->getMessages(), [$this, 'escapeHtml']) ?>
                </div>
            </div>

            <div style="display: flex; justify-content: flex-end; gap: 12px; margin-top: 24px; flex-wrap: wrap;">
                <a
                    href="<?= $this->url('product') ?>"
                    style="
                        display: inline-flex;
                        align-items: center;
                        justify-content: center;
                        padding: 12px 18px;
                        border-radius: 8px;
                        background: #e5e7eb;
                        color: #111827;
                        text-decoration: none;
                        font-weight: 600;
                    "
                >
                    Cancelar
                </a>

                <button
                    type="submit"
                    style="
                        padding: 12px 18px;
                        border: 0;
                        border-radius: 8px;
                        background: #2563eb;
                        color: #fff;
                        font-weight: 600;
                        cursor: pointer;
                    "
                >
                    <?= $isEdit ? 'Salvar alterações' : 'Criar produto' ?>
                </button>
            </div>
        <?= $this->form()->closeTag() ?>
    </div>
</main>

<script>
    (function () {
        const selector = document.getElementById('category-selector');
        const container = document.getElementById('selected-categories');

        if (!selector || !container) {
            return;
        }

        function hasCategory(categoryId) {
            return container.querySelector('[data-category-id="' + CSS.escape(categoryId) + '"]') !== null;
        }

        function bindRemove(button) {
            button.addEventListener('click', function () {
                const item = button.closest('.selected-category-item');

                if (item) {
                    item.remove();
                }
            });
        }

        function createCategoryItem(categoryId, categoryName) {
            const item = document.createElement('div');
            item.className = 'selected-category-item';
            item.setAttribute('data-category-id', categoryId);
            item.style.display = 'inline-flex';
            item.style.alignItems = 'center';
            item.style.gap = '10px';
            item.style.padding = '10px 12px';
            item.style.borderRadius = '999px';
            item.style.background = '#eff6ff';
            item.style.border = '1px solid #bfdbfe';
            item.style.color = '#1e3a8a';
            item.style.fontSize = '14px';
            item.style.fontWeight = '600';

            const label = document.createElement('span');
            label.textContent = categoryName;

            const removeButton = document.createElement('button');
            removeButton.type = 'button';
            removeButton.className = 'remove-category-button';
            removeButton.textContent = '×';
            removeButton.style.width = '24px';
            removeButton.style.height = '24px';
            removeButton.style.border = '0';
            removeButton.style.borderRadius = '999px';
            removeButton.style.background = '#dbeafe';
            removeButton.style.color = '#1d4ed8';
            removeButton.style.fontWeight = '700';
            removeButton.style.cursor = 'pointer';
            removeButton.style.lineHeight = '1';

            const hidden = document.createElement('input');
            hidden.type = 'hidden';
            hidden.name = 'categories[]';
            hidden.value = categoryId;

            item.appendChild(label);
            item.appendChild(removeButton);
            item.appendChild(hidden);

            bindRemove(removeButton);

            return item;
        }

        container.querySelectorAll('.remove-category-button').forEach(bindRemove);

        selector.addEventListener('change', function () {
            const categoryId = selector.value;
            const categoryName = selector.options[selector.selectedIndex]?.text ?? '';

            if (!categoryId) {
                return;
            }

            if (!hasCategory(categoryId)) {
                container.appendChild(createCategoryItem(categoryId, categoryName));
            }

            selector.value = '';
        });
    })();
</script>

<?= $this->partial('layout/footer') ?>