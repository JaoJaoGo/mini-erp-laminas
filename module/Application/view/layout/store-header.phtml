<?php

declare(strict_types=1);

/** @var array{name:string, categoryId:?int, sort:string, inStock:bool} $filters */
$filters = $this->filters ?? [
    'name' => '',
    'categoryId' => null,
    'sort' => 'latest',
    'inStock' => false,
];
?>

<header style="background: #131921; color: #fff; border-bottom: 1px solid rgba(255,255,255,0.08);">
    <div style="max-width: 1440px; margin: 0 auto; padding: 16px 24px;">
        <div style="display: grid; grid-template-columns: 220px minmax(0, 1fr) auto; gap: 18px; align-items: center;">
            <a
                href="<?= $this->url('store') ?>"
                style="text-decoration: none; color: #fff; display: inline-flex; flex-direction: column;"
            >
                <span style="font-size: 12px; color: #fbbf24; letter-spacing: 0.08em; text-transform: uppercase;">
                    Mini ERP
                </span>
                <strong style="font-size: 24px; line-height: 1.1;">
                    Loja
                </strong>
            </a>

            <form method="get" action="<?= $this->url('store') ?>" style="display: grid; grid-template-columns: minmax(0, 1fr) 130px 150px; gap: 8px;">
                <input
                    type="text"
                    name="name"
                    value="<?= $this->escapeHtmlAttr($filters['name']) ?>"
                    placeholder="Buscar produtos..."
                    style="width: 100%; padding: 12px 14px; border: 0; border-radius: 10px; box-sizing: border-box; font-size: 14px;"
                >

                <select
                    name="sort"
                    style="padding: 12px 14px; border: 0; border-radius: 10px; box-sizing: border-box; font-size: 14px;"
                >
                    <option value="latest" <?= $filters['sort'] === 'latest' ? 'selected' : '' ?>>Mais recentes</option>
                    <option value="price_asc" <?= $filters['sort'] === 'price_asc' ? 'selected' : '' ?>>Menor preço</option>
                    <option value="price_desc" <?= $filters['sort'] === 'price_desc' ? 'selected' : '' ?>>Maior preço</option>
                    <option value="name_asc" <?= $filters['sort'] === 'name_asc' ? 'selected' : '' ?>>Nome A-Z</option>
                </select>

                <button
                    type="submit"
                    style="padding: 12px 16px; border: 0; border-radius: 10px; background: #febd69; color: #111827; font-weight: 700; cursor: pointer;"
                >
                    Buscar
                </button>

                <?php if ($filters['categoryId'] !== null): ?>
                    <input type="hidden" name="category" value="<?= (int) $filters['categoryId'] ?>">
                <?php endif; ?>

                <?php if ($filters['inStock']): ?>
                    <input type="hidden" name="inStock" value="1">
                <?php endif; ?>
            </form>

            <div style="display: flex; align-items: center; gap: 18px; justify-content: flex-end;">
                <a
                    href="#"
                    style="text-decoration: none; color: #fff; display: inline-flex; flex-direction: column; align-items: flex-start;"
                >
                    <span style="font-size: 12px; color: #cbd5e1;">Seu</span>
                    <strong style="font-size: 16px;">Carrinho (0)</strong>
                </a>
            </div>
        </div>
    </div>
</header>